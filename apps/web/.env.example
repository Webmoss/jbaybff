# Symlink or copy the values you need from the workspace-root `.env.example`
# into `apps/web/.env.local` for app-specific dev. Both Next.js apps inherit
# the workspace-root `.env` automatically because PM2/Node load it via the
# ecosystem file at deploy time.
PORT=3000
